{
    "name": "lobby-app",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/game-lobby/app/src",
    "projectType": "application",
    "tags": [
        "scope:lobby",
        "app:lobby-app",
        "type:app",
        "tier:app",
        "environment:production",
        "environment:demo",
        "environment:prod",
        "environment:dev"
    ],
    "targets": {
        "build": {
            "executor": "@nx/vite:build",
            "outputs": ["{options.outputPath}"],
            "defaultConfiguration": "production",
            "options": {
                "outputPath": "dist/apps/lobby/app"
            },
            "configurations": {
                "development": {
                    "mode": "development"
                },
                "production": {
                    "mode": "production"
                }
            }
        },
        "serve": {
            "executor": "@nx/vite:dev-server",
            "defaultConfiguration": "development",
            "options": {
                "buildTarget": "lobby-app:build"
            },
            "configurations": {
                "development": {
                    "buildTarget": "lobby-app:build:development",
                    "hmr": true
                },
                "production": {
                    "buildTarget": "lobby-app:build:production",
                    "hmr": false
                }
            }
        },
        "lint": {},
        "lint:fix": {},
        "typecheck": {},
        "test": {
            "executor": "@nx/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "apps/lobby/app/jest.config.ts"
            }
        },
        "preview": {
            "dependsOn": ["build"],
            "executor": "@nx/vite:preview-server",
            "defaultConfiguration": "development",
            "options": {
                "buildTarget": "lobby-app:build"
            },
            "configurations": {
                "development": {
                    "buildTarget": "lobby-app:build:development"
                },
                "production": {
                    "buildTarget": "lobby-app:build:production"
                }
            }
        }
    }
}
