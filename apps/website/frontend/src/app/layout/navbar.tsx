import styled from "@emotion/styled"

const Container = styled.nav`
  height: 5rem;
  border-bottom: 0.3rem solid lightgray;
`

export const Navbar = () => {
  return (
    <Container>
      Navbar
    </Container>
  )
}
