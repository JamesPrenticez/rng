{
  "name": "jump-quest-app",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/jump-quest/app/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/jump-quest/app"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "jump-quest-app:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "jump-quest-app:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "jump-quest-app:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "dependsOn": ["build"],
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "jump-quest-app:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "jump-quest-app:build:development"
        },
        "production": {
          "buildTarget": "jump-quest-app:build:production"
        }
      }
    },
    "serve-static": {
      "executor": "@nx/web:file-server",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "jump-quest-app:build",
        "spa": true
      }
    }
  }
}
