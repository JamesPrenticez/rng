import styled from '@emotion/styled';

const Container = styled.div`
  display: flex;
  flex-direction: column;
  flex-grow: 1;
`;

export const MessengerHome = () => {
  return <Container>Messenger Home</Container>;
};
