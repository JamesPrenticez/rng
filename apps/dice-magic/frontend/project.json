{
  "name": "dice-magic-frontend",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/dice-magic/frontend/src",
  "projectType": "application",
  "tags": [
    "scope:dice-magic",
    "app:dice-magic-frontend",
    "type:app",
    "environment:production",
    "environment:demo",
    "environment:dev"
  ],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/dice-magic/frontend"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "dice-magic-frontend:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "dice-magic-frontend:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "dice-magic-frontend:build:production",
          "hmr": false
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["tsc -p tsconfig.app.json --noEmit --incremental"],
        "cwd": "{projectRoot}",
        "forwardAllArgs": false
      }
    },
    "preview": {
      "dependsOn": ["build"],
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "dice-magic-frontend:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "dice-magic-frontend:build:development"
        },
        "production": {
          "buildTarget": "dice-magic-frontend:build:production"
        }
      }
    }
  }
}
